# --- LiveKit (cliente/agents) ---
livekit>=0.18.0
livekit-api>=1.0.5
livekit-agents>=0.7.0

# --- Utilidades ---
python-dotenv>=1.0.1
ujson>=5.10.0
aiohttp>=3.10.0

# --- Provedores que você disse usar pelas ENVs ---
# GOOGLE_API_KEY normalmente é usado com Google Generative AI:
google-generativeai>=0.7.2

# ELEVEN_API_KEY (SDK oficial)
elevenlabs>=1.6.1
# Core LiveKit
livekit-agents>=0.7.1

# Plugins usados no seu import
livekit-plugins-noise-cancellation>=0.2.1
livekit-plugins-silero>=0.2.0
livekit-plugins-google>=0.2.0

# Áudio e utilidades
soundfile>=0.12.1
numpy>=1.26
python-dotenv>=1.0.1
aiohttp>=3.10.5

# Dependências do plugin Google (caso não venham como sub-dep)
google-cloud-speech>=2.26.0
google-cloud-texttospeech>=2.16.3